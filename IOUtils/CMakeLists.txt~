project(libIOUtils)
cmake_minimum_required(VERSION 2.8)

set(LIBRARY_OUTPUT_PATH ${PROJECT_BINARY_DIR}/lib)
set(src_dir src)
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -std=c++11")
include_directories(include)
include_directories(../include)
add_library(IOUtils STATIC ${src_dir}/IO.cpp) 
